#include <iostream>
#include <WinSock2.h>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
#pragma comment(lib, "ws2_32.lib")

bool Initialize() {
	WSATA
}
int main() {
	Intialize();
	cout<<"Ready the program"<<endl;
	return 0;
}
