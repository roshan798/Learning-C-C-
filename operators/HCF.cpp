//LCM of more than two numbers
#include<iostream>
#include<vector>
using namespace std;
int main()
{
	int num;
	cout<<"Enter the number of elements in array : ";
	cin>>num;
	vector<int> arr(num);
	cout<<endl<<"Enter "<<num<<" items : ";
	for(int i=0;i<num;i++)
		cin>>arr[i];
	int HCF=0,min=INT_MAX;
	
	
}
